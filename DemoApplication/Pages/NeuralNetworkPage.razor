@page "/neuralnet"
@using BlazorML5
@inject IJSInProcessRuntime Runtime



    @code {

        protected override async Task OnInitializedAsync()
        {
            ML5.Init(Runtime as IJSRuntime);

            NeuralNetwork neuralNetwork = await NeuralNetwork.Create(5,2);
        }
}
