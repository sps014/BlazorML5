@page "/posenet"
@inject IJSRuntime Runtime

@code
{
    ML5.PoseNet net;
    protected override void OnInitialized()
    {
        net = new ML5.PoseNet(Runtime);
        base.OnInitialized();
    }
}